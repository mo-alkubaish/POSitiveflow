import { SanitizationPipePipe } from './sanitization-pipe.pipe';

describe('SanitizationPipePipe', () => {
  it('should be defined', () => {
    expect(new SanitizationPipePipe()).toBeDefined();
  });
});
