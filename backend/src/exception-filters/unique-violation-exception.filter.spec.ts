import { UniqueViolationExceptionFilter } from './unique-violation-exception.filter';

describe('UniqueViolationExceptionFilter', () => {
  it('should be defined', () => {
    expect(new UniqueViolationExceptionFilter()).toBeDefined();
  });
});
